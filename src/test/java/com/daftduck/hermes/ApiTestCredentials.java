package com.daftduck.hermes;

public interface ApiTestCredentials {

    // These are only test credentials, so we can use the anon usage service that
    // TfL provides and is rate limited, but still gives us the same data.
    String API_ID = "";
    String API_KEY = "";

}
